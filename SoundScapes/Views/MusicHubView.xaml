<UserControl x:Class="SoundScapes.Views.MusicHubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:SoundScapes.Views" xmlns:viewmodels="clr-namespace:SoundScapes.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MusicHubViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="5"/>
            <ColumnDefinition Width="3"/>
            <ColumnDefinition MinWidth="5"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" BorderThickness="1" BorderBrush="White" CornerRadius="6" Padding="5" Margin="5" SnapsToDevicePixels="True">
            <views:PlaylistView/>
        </Border>
        <GridSplitter Grid.Column="1" Margin="0,15,0,15" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        <Border Grid.Column="2" BorderThickness="1" BorderBrush="White" CornerRadius="6" Padding="5" Margin="5" SnapsToDevicePixels="True">
            <views:SearchView />
        </Border>
    </Grid>
</UserControl>